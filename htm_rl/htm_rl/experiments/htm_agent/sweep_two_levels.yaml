project: basalganglia
program: ../../agents/htm/htm_agent.py
method: grid
metric:
  goal: minimize
  name: av_steps
parameters:
  seed:
    values: [12312, 3333, 105482]
  environment.seed:
    values: [2304, 2312, 222, 213293]

command:
  - ${env}
  - python
  - ${program}
  - two_levels_default
  - ${args}

