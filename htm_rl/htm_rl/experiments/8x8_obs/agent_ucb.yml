_type_: ucb

sa_encoder: q

qvn:
  discount_factor: &discount_factor .97
  learning_rate: [.1, .995]

eligibility_traces:
  trace_decay: .75
  discount_factor: *discount_factor

ucb_estimate:
  visit_decay: .92
  ucb_exploration_factor: [.3, .997]
