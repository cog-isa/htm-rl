_type_: q

sa_encoder:

qvn:
  discount_factor: &discount_factor .95
  learning_rate: [.1, .995]

eligibility_traces:
  trace_decay: [.75, .99]
  discount_factor: *discount_factor

softmax_temp: [.0, .0]
exploration_eps: [.2, .99]
