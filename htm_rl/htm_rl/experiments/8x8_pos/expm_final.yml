seed: 8713
n_seeds: 12

experiment:
  project: test_1f_pos_8x8
  n_episodes: 500

env:
  - pos

agent:
  - rnd
  - ucb

run_results_processor: !RunResultsProcessor
  env_name: biogwlab
  moving_average: 4
  verbosity: 1
  test_dir: # leave empty for default behavior - output to the config's dir
